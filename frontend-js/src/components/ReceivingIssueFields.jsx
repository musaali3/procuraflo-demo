export const RECEIVING_REASONS=['Damaged goods','Missing quantity','Short delivery','Incorrect item','Quality failure','Rejected item','Supplier backorder','Other'];
export default function ReceivingIssueFields({line,onChange,inspection=false}){
 const damaged=inspection?'damaged_quantity':'damaged_qty',reason=inspection?'issue_reason':'receiving_issue_reason',notes=inspection?'remarks':'receiving_notes';
 return <div className="col-span-12 grid grid-cols-2 gap-3 rounded border border-amber-200 p-3"><label className="text-sm">Damaged quantity (included in {inspection?'failed':'rejected'})<input className="input" type="number" min="0" step="any" data-field={damaged} value={line[damaged]||''} onChange={e=>onChange(damaged,Number(e.target.value))}/></label>{!inspection&&<label className="text-sm">Missing / short quantity this delivery<input className="input" type="number" min="0" step="any" data-field="short_qty" value={line.short_qty||''} onChange={e=>onChange('short_qty',Number(e.target.value))}/></label>}<label className="text-sm">Receiving issue / reason<select className="input" data-field={reason} value={line[reason]||''} onChange={e=>onChange(reason,e.target.value)}><option value="">No exception recorded</option>{RECEIVING_REASONS.map(reason=><option key={reason}>{reason}</option>)}</select></label><label className="text-sm">Notes / comments<textarea className="input" data-field={notes} value={line[notes]||''} onChange={e=>onChange(notes,e.target.value)}/></label></div>;
}
